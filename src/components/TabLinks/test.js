/**
 * @format
 */

import 'react-native';
import React from 'react';
import renderer from 'react-test-renderer';
import TabLinks from './TabLinks';


it('renders correctly', () => {
  renderer.create(<TabLinks />);
});
