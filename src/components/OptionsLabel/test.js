/**
 * @format
 */

import 'react-native';
import React from 'react';
import renderer from 'react-test-renderer';
import OptionsLabel from './OptionsLabel';


it('renders correctly', () => {
  renderer.create(<OptionsLabel />);
});
