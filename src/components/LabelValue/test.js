/**
 * @format
 */

import 'react-native';
import React from 'react';
import renderer from 'react-test-renderer';
import LabelValue from './LabelValue';


it('renders correctly', () => {
  renderer.create(<LabelValue />);
});
