/**
 * @format
 */

import 'react-native';
import React from 'react';
import renderer from 'react-test-renderer';
import DoadoraColeta from './DoadoraHistorico';


it('renders correctly', () => {
  renderer.create(<DoadoraColeta />);
});
